{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Zephyr GDB",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/zephyr/zephyr.exe",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb"
        }
    ]
}