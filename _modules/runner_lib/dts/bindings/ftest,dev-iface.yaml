
description:  The driver for accessing GPIO interfaces of entities in the FTest framework.

compatible: "ftest,dev-iface"

properties:
  remote-label:
    type: string
    description: The label by which the GPIO interface is referenced in the entity.
    required: true